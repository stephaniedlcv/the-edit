You are working on THE EDIT / Closet OS.

Current branch:
sprint-2-security-stability

Context:
Sprint 1 is complete and merged into main.
The app now uses real data for Home and Wishlist, wardrobe archiving uses `item_status`, and Planner is honestly marked as Preview.

Goal:
Complete Sprint 2 — “Security + Stability”.

Do not redesign the UI.
Do not add Beauty OS, AI features, weather features, shopping automation, or multi-user support.
Do not do large refactors.
Keep this sprint focused on security, documentation, and stability.

Tasks:

1. Add a `typecheck` script

* Update `package.json` so we can run:

  * `npm run typecheck`
* It should run:

  * `tsc --noEmit`

2. Protect mutable API routes with a simple app-level secret

* Add a small reusable server helper for checking an optional API secret.
* Use an environment variable such as:

  * `THE_EDIT_API_SECRET`
* If `THE_EDIT_API_SECRET` is not set, preserve current local development behavior.
* If `THE_EDIT_API_SECRET` is set, require matching header on mutable routes.
* Header name can be:

  * `x-the-edit-secret`
* Protect only mutable routes for now:

  * POST `/api/closet/items`
  * PATCH `/api/closet/items/[id]`
  * POST/PATCH image upload route if applicable
  * POST `/api/outfits/saved`
  * DELETE or soft-delete routes if any exist
* Do not break read-only pages.
* Do not implement full auth or multi-user auth in this sprint.

3. Filter deleted saved outfits in API GET

* Inspect `/api/outfits/saved`.
* Ensure GET does not return rows where `status = 'deleted'`.
* If a data layer already filters this but the API route does not, align the API route.

4. Wishlist signed image URLs

* Inspect wishlist data loading.
* If `wishlist_item_images.image_path` uses private Supabase storage, add signed URL handling similar to wardrobe images.
* Avoid duplicating logic unnecessarily.
* Do not redesign wishlist UI.
* If wishlist images are already public or not used, document the finding.

5. Review photo scripts vs schema

* Inspect scripts related to closet photos, including:

  * `sync-closet-photos`
  * upload/audit scripts if present
* Confirm whether scripts reference columns that do not exist in migrations, such as `photo_filename` or `drive_photo_path`.
* Do not make a risky migration unless necessary.
* Prefer making scripts harmless, clearly documented, and aligned with the current schema.
* If a script is legacy, add comments or update README to mark it as legacy.

6. README and `.env.example`

* Replace the default README with a real project README.
* Include:

  * What THE EDIT is
  * Current feature status
  * Stack
  * Local setup
  * Scripts
  * Environment variables
  * Supabase notes
  * Migration notes
  * What is real vs preview
  * Roadmap
* Add `.env.example` with all required/optional variables:

  * NEXT_PUBLIC_SUPABASE_URL
  * NEXT_PUBLIC_SUPABASE_ANON_KEY
  * SUPABASE_SERVICE_ROLE_KEY
  * THE_EDIT_API_SECRET
  * any calendar-related variables already used by the app
* Do not include real secrets.

7. Calendar Layer sanity check

* The project already has calendar-related code and `/api/calendar`.
* Do not implement a new calendar integration.
* Inspect and document the current Calendar Layer:

  * what exists
  * what it reads
  * whether Planner consumes it
  * what is still preview/mock
* Update README accordingly.
* Only make tiny copy changes if the UI currently misrepresents what is connected.

Verification:
Run:

* npm run lint
* npm run typecheck
* npm run build

Expected:

* lint passes
* typecheck passes
* build passes
* Home and Wishlist remain dynamic
* Planner can remain static/preview

Deliver:

* Files changed
* Summary of fixes
* Exact verification output
* Any remaining risks
* Any manual QA steps needed
