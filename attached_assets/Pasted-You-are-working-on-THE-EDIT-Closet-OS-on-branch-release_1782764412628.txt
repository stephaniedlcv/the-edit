You are working on THE EDIT / Closet OS on branch `release-0.1-stabilization`.

The lint baseline is already clean:

* `npm run lint` passes
* `npx tsc --noEmit` passes
* `npm run build` passes

Now start Sprint 1 — “Real Data + Trust Fixes”.

Do not redesign the UI.
Do not add new features.
Do not do large refactors unless required for the specific fixes below.
Keep the existing visual style and current user experience.

Goal:
Make the app truthful and aligned with Supabase data instead of mock/static data.

Tasks:

1. Fix wardrobe archiving

* Find the conflict between `is_archived` and `item_status`.
* Use `item_status` as the canonical source of truth.
* Update wardrobe queries so the closet only shows `item_status = 'active'`.
* Add a Supabase migration that backfills:

  * `item_status = 'archived'` where `is_archived = true`
  * `item_status = 'active'` where `is_archived = false` or null and `item_status` is null
* Do not drop `is_archived` yet.
* Make sure archiving from the UI causes the item to disappear from the active closet.

2. Make Home use real data

* Update `src/app/page.tsx` so it no longer imports mock owned items, mock wishlist items, or mock outfit counts for dashboard stats.
* Use real data fetchers where available:

  * `getWardrobeItems()`
  * `getWishlistItems()`
  * `getSavedOutfits()` or the existing saved outfits data layer if available
* Add `export const dynamic = "force-dynamic"` if needed.
* If real data is empty, show an honest empty state or zero count, not fake mock stats.
* Preserve the current visual layout.

3. Make Wishlist visual board use real data

* Update `src/app/wishlist/page.tsx` to pass fetched wishlist items into `WishlistPriorityBoard`.
* Update `src/components/wishlist-priority-board.tsx` to accept `items` as a prop.
* Remove direct import/use of `mockWishlistItems` inside the board.
* Preserve existing filters, sorting, grouping, and UI.
* Add a real empty state when there are no wishlist items.
* Add `export const dynamic = "force-dynamic"` if needed so Wishlist does not prerender stale data.

4. Make Planner honest

* If Planner still uses mock/static data, update the copy so the page clearly says “Planner Preview”.
* Do not imply Apple Calendar, weather, or real outfit logs are connected unless they actually are.
* Do not implement calendar integration yet.
* Preserve the current design.

5. Verification
   Run:

* `npm run lint`
* `npx tsc --noEmit`
* `npm run build`

Expected:

* lint passes
* typecheck passes
* build passes
* `/` and `/wishlist` should become dynamic if they now depend on real server data

Deliver:

* Files changed
* Summary of what was fixed
* Migration SQL created
* Commands run and exact results
* Any remaining risks
